services:
  argos_cly_app_prod:
    container_name: argos_cly_app_prod
    image: argos-cly-app-prod
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "8088:8088"
    depends_on:
      - argos_cly_postgres_prod
    profiles:
      - production

  argos_cly_postgres_prod:
    container_name: argos_cly_postgres_prod
    image: postgres:latest
    environment:
      POSTGRES_DB: ${PROD_DB_NAME}
      POSTGRES_USER: ${PROD_DB_USERNAME}
      POSTGRES_PASSWORD: ${PROD_DB_PASSWORD}
    ports:
      - "5435:5432"
    volumes:
      - postgres_production_data:/var/lib/postgresql/data
    profiles:
      - production
    env_file:
      - .env

volumes:
  postgres_production_data: {}
