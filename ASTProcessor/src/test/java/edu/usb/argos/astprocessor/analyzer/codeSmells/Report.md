## Example:

### Java File Entry and Rule Definition
For this example, these input classes will be used:

````java
public class MathOperations {
    public int sumMultiplication(int a, int b) {
        int sumResult = a + b;
        int result = sumResult * 3;
        System.out.println("SumMultiplication result: " + result);
        return result;
    }

    public int sumDivision(int a, int b) {
        int sumResult = a + b;
        int result = (int) Math.ceil(sumResult / 2.0);
        System.out.println("SumDivision result: " + result);
        return result;
    }
}
````

```java
public class SimpleMathOperations {
    public int sum(int a, int b) {
        if (a < 0 || b < 0) {
            throw new IllegalArgumentException("Values must be non-negative");
        }
        int result = a + b;
        System.out.println("Sum result: " + result);
        return result;
    }
}
```

And these code smells rules, so that analysis is done:

```
1. Analysis of Excessive Parameters with a maximum size of 1
2. Analysis of Long Methods with a maximum size of 3
3. Analysis of duplicate code between all classes
```

### Result for each class
Analyzers generate reports for each class, each report for each class indicates the type of code and message that does not comply with any analyzer.

````bash
SimpleMathOperations.java
CodeAnalysisReport(startLine=2, endLine=9, type=METHOD_TOO_LONG, message=The method is too long, consider breaking it down into smaller, reusable methods.)
CodeAnalysisReport(startLine=2, endLine=9, type=EXCESSIVE_PARAMETERS, message=The method has too many parameters, making it difficult to understand and use.)
CodeAnalysisReport(startLine=2, endLine=9, type=DUPLICATED_CODE, message=Similar code was detected in file MathOperations.java/sumMultiplication, consider abstracting it or reusing functions.)
CodeAnalysisReport(startLine=2, endLine=9, type=DUPLICATED_CODE, message=Similar code was detected in file MathOperations.java/sumDivision, consider abstracting it or reusing functions.)

MathOperations.java
CodeAnalysisReport(startLine=9, endLine=14, type=DUPLICATED_CODE, message=Similar code was detected in file SimpleMathOperations.java/sum, consider abstracting it or reusing functions.)
CodeAnalysisReport(startLine=2, endLine=7, type=EXCESSIVE_PARAMETERS, message=The method has too many parameters, making it difficult to understand and use.)
CodeAnalysisReport(startLine=2, endLine=7, type=DUPLICATED_CODE, message=Similar code was detected in file SimpleMathOperations.java/sum, consider abstracting it or reusing functions.)
CodeAnalysisReport(startLine=2, endLine=7, type=METHOD_TOO_LONG, message=The method is too long, consider breaking it down into smaller, reusable methods.)
CodeAnalysisReport(startLine=2, endLine=7, type=DUPLICATED_CODE, message=Similar code was detected in file MathOperations.java/sumDivision, consider abstracting it or reusing functions.)
CodeAnalysisReport(startLine=9, endLine=14, type=DUPLICATED_CODE, message=Similar code was detected in file MathOperations.java/sumMultiplication, consider abstracting it or reusing functions.)
CodeAnalysisReport(startLine=9, endLine=14, type=METHOD_TOO_LONG, message=The method is too long, consider breaking it down into smaller, reusable methods.)
CodeAnalysisReport(startLine=9, endLine=14, type=EXCESSIVE_PARAMETERS, message=The method has too many parameters, making it difficult to understand and use.)
````

So if we look for these reports in the file where they belong, we can render the code analysis with a message.
Let's take the first reports generated by file, find the code range of the report and render it with a message

### File: SimpleMathOperations.java
**Long method analysis:**
```java
public int sum(int a, int b) {
    if (a < 0 || b < 0) {
        throw new IllegalArgumentException("Values must be non-negative");
    }
    int result = a + b;
    System.out.println("Sum result: " + result);
    return result;
}
```
```textmate
The method is too long, consider breaking it down into smaller, reusable methods.
```
### File: MathOperations.java
**Duplicate Code Analysis:**
```java
public int sumDivision(int a, int b) {
    int sumResult = a + b;
    int result = (int) Math.ceil(sumResult / 2.0);
    System.out.println("SumDivision result: " + result);
    return result;
}
```
```textmate
Similar code was detected in file SimpleMathOperations.java/sum, consider abstracting it or reusing functions.
```
---
**Excessive Parameters Analysis:**
```java
public int sumMultiplication(int a, int b) {
    int sumResult = a + b;
    int result = sumResult * 3;
    System.out.println("SumMultiplication result: " + result);
    return result;
}
```
```textmate
The method has too many parameters, making it difficult to understand and use.
```
